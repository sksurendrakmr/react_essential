import React from 'react'

/**
 * Ref forwarding is a technique for automatically passing a ref through a component
 * to one of its children
 * 
 * With forward ref, we allow the parent component to directly reference the child component native element
 */

export const index = () => {
    return (
        <div>index</div>
    )
}
